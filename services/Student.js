const { v4: uuid } = require("uuid");
const jwt = require("jsonwebtoken");
const bcrypt = require("bcrypt");

class Student {
  getStudent(ra) {}

  createStudent(ra) {}
}

module.exports = {
  Student,
};
