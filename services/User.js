const { v4: uuid } = require('uuid');
const jwt = require('jsonwebtoken');
const bcrypt = require('bcrypt');

class User {
    constructor () {
        
    }

    createUser() {
        
    }
}

module.exports = {
    User
}